import React from 'react'

const JSXRules = () => {
  return (
    <div>
      <h1>JSX Rules </h1>
      <p> jsx must return a single parent element </p>
        <p> use camelCase for html attributes </p>  
        <p> className instead of class </p>
        <p>jsx elements must be self-closing if they have no children</p>
        <p>Expression must be wrapped in curly braces </p>
    </div>
  )
}

export default JSXRules
